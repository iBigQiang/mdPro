name: Create Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Extract Changelog
        id: changelog
        run: |
          echo "Extracting latest changelog from 升级日志文档.md"
          
          # 使用 Python 脚本更可靠地提取第一个 ## 块
          CHANGELOG=$(python3 -c "
          import re
          with open('升级日志文档.md', 'r', encoding='utf-8') as f:
              content = f.read()
              match = re.search(r'##[\s\S]*?(?=\n##|$)', content)
              if match:
                  print(match.group(0).strip())
              else:
                  print('No changelog entry found.')
          ")

          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          echo "$CHANGELOG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          body: |
            # mdPro ${{ github.ref_name }} 发布🎈

            > 一款高度简洁的微信 Markdown 编辑器

            ${{ steps.changelog.outputs.changelog }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
