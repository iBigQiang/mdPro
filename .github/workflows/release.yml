name: Create Release

on:
  push:
    tags:
      - "v*"

jobs:
  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Extract Changelog
        id: changelog
        run: |
          echo "Extracting latest changelog from 升级日志文档.md"
          # 提取最后一个 ## 及其之后的所有内容
          CHANGELOG=$(sed -n 'H; /^## /h; ${g;p;}' "升级日志文档.md")

          if [ -z "$CHANGELOG" ]; then
            CHANGELOG="No changelog entry found in 升级日志文档.md"
          fi

          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          echo "$CHANGELOG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          body: |
            # mdPro ${{ github.ref_name }} 发布🎈

            > 一款高度简洁的微信 Markdown 编辑器

            ${{ steps.changelog.outputs.changelog }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
